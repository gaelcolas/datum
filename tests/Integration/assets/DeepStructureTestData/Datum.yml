ResolutionPrecedence:
  - AllNodes\$($Node.NodeName)
  - Baselines\Default

default_lookup_options: MostSpecific

default_json_depth: 8

lookup_options:
  DeepConfig:
    merge_hash: deep
    merge_options:
      knockout_prefix: --
